#!/usr/bin/node
/* Write a script that prints two arguments passed to it, in the following format: “ is ” */
const txt1 = process.argv[2];
const txt2 = process.argv[3];
console.log(txt1 + ' is ' + txt2);
